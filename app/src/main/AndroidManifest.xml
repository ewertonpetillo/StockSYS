<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrador.stocksys">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icone6"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UsuarioActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity
            android:name=".InicialActivity"
            android:label="@string/title_activity_inicial"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ServidorActivity" />
        <activity android:name=".ListaServidor" />
        <activity android:name=".MaterialActivity" />
        <activity android:name=".ListaEstoque" />
        <activity android:name=".ListaUsuarios" />
        <activity android:name=".SaidaMaterial" />
        <activity android:name=".AltExcServidor"></activity>
    </application>

</manifest>